import { DoiMauChuDirective } from './doi-mau-chu.directive';

describe('DoiMauChuDirective', () => {
  it('should create an instance', () => {
    const directive = new DoiMauChuDirective();
    expect(directive).toBeTruthy();
  });
});
